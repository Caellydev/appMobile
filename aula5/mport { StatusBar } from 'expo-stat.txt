import { StatusBar } from 'expo-status-bar';
import { FlatList, Image, StyleSheet, Text, View } from 'react-native';

export default function App() {
  return (
    <View style={styles.container}>
      <Text>Vamos Analisar!</Text>


        <FlatList
          data={catalogoAnimais}
          renderItem={({item})=>
            <View>
              <Text style={styles.animal}>°{item.animal}</Text>
              <Text style={styles.classe}>°{item.classe}</Text>
              <Text style={styles.peso}>°{item.peso}</Text>
              <Image style={styles.img} source={item.img}/>
              </View>
          } 
          />

          <StatusBar style="auto" />
        </View>
    );
  }
//ele criou uma const estilo aqui 
const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#d3e8f5',
    alignItems: 'center',
    justifyContent: 'center',
  },
  title:{
    fontSize: 50,
    fontWeight: 'bold',
    textAlign: 'center'
  },
  img:{
    borderWidth: 1,
    borderColor: 'white',
    height: 250,
    width: 300,
  }
});
//aqui o catálogo

const catalogoAnimais = [
  {
    uid: 1,
    animal: 'Leão Marinho',
    classe: 'marinho',
    peso: '100kg',
    img: require('./assets/leao-marinho.jpg')
  }
];